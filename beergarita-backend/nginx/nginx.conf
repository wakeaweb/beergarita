events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;
    client_max_body_size 20M;

    # Redirect HTTP → HTTPS
    server {
        listen 80;
        server_name api.beergarita.com.tr;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS server
    server {
        listen 443 ssl;
        server_name api.beergarita.com.tr;

        ssl_certificate     /etc/letsencrypt/live/api.beergarita.com.tr/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.beergarita.com.tr/privkey.pem;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;

        # API → backend server container
        location /api/v1/ {
            proxy_pass http://server:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # File uploads → backend server container
        location /uploads/ {
            proxy_pass http://server:3000;
            proxy_set_header Host $host;
        }

        # Admin SPA → backend server container
        location /admin/ {
            proxy_pass http://server:3000;
            proxy_set_header Host $host;
        }

        # Root redirect to admin
        location / {
            return 301 /admin/;
        }
    }
}
